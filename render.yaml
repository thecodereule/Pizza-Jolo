services:
  - type: web
    name: pizza-jolo-backend
    runtime: node
    plan: free
    region: frankfurt
    rootDir: app/pizza-express
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        sync: false
      - key: MONGO_DB_NAME
        sync: false
      - key: JWT_SECRET
        sync: false
